import * as THREE from 'three';

export const injectWaterShader = (shader: THREE.Shader) => {
  // Kosongkan fungsi ini untuk menonaktifkan shader gelombang.
  // Tidak ada modifikasi yang akan diterapkan pada material.
};
